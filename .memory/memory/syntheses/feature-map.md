# Feature map

Current product capabilities inferred from durable repository evidence:
- User Authentication: User Authentication - Register, login, logout with JWT tokens
- Tournament Management: Tournament Management - Create, join, and manage knockout/league tournaments
- Match Results: Match Results - Submit scores with screenshots, confirm/dispute flow
- Real-time Chat: Real-time Chat - Socket.IO powered tournament chat
- Modern UI: Modern UI - Dark theme with neon accents, glassmorphism
- JWT authentication with token blacklist: JWT authentication with token blacklist
- Rate limiting (100 req/15min global, 10 req/15min auth): Rate limiting (100 req/15min global, 10 req/15min auth)
- SQL injection protection: SQL injection protection

Update this synthesis when features are added, removed, renamed, or replaced.
